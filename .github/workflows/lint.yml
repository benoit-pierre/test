name: Lint

permissions:
  contents: read

on:
  workflow_call:
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:

  lint:

    name: Lint

    runs-on: ubuntu-latest

    env:
      CLICOLOR_FORCE: '1'

    steps:

      - name: Lint
        run: ":"

# vim: foldmethod=marker foldlevel=0
